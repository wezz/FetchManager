import FetchManager from './FetchManager';

test('Runs without crashing', () => {
  // var fetchManager = new FetchManager();
  // const url = fetchManager.CompileUrl({
  //   url: 'https://google.com',
  //   querystring: {
  //     'name': 'john doe'
  //   }
  // });
  // console.log(url);
});
